project_name: EdgeGen
seed: 42
device: cpu
training:
  epochs: 100
  batch_size: 128
  latent_dim: 100
  lr: 0.0002
  beta1: 0.5
  sample_every: 10
compression:
  prune_ratios: [0.2, 0.4, 0.6]
benchmark:
  batch_size: 32
  warmup_iters: 5
  measure_iters: 20
adaptive_inference:
  threshold_ms: 25.0
